- name: Destroy {{ elb_role_type }} load balancer
  ec2_application_lb:
    name: "{{ item.name }}"
    region: "{{ region }}"
    profile: "{{ aws_account_name }}"
    state: absent
  with_items: "{{ loadbalancers }}"

